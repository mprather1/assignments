<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <li class="navbar-brand active">
        <% if logged_in? %>
          <%= link_to_unless_current "#{current_user.name}", access_index_path %>
        <% else %>
          <a href="#">Classroom Management</a>
          <% end %>
        </li>


    </div>

      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <% if user_admin? %>
            <li class="active"><%= link_to 'Assignments', assignments_path %></li>
            <li class="active"><%= link_to 'Students', users_path %></li>
            <li class="active"><%= link_to 'Classes', classrooms_path %></li>
          <% else %>
            <li class="active"><%= link_to 'Assignments', user_assignments_path(current_user) %></li>
            <li class="active"><%= link_to 'Classes', user_classrooms_path(current_user) %></li>
          <% end %>
          <li class="active"><%= link_to 'Logout', access_logout_path %></li>
        <% end %>
      </ul>

  </div>

</nav>
